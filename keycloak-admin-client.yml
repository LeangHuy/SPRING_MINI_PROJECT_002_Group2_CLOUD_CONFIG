server:
  port: 8083
spring:
  application:
    name: keycloak-admin-client
  #Resource server configuration
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/mini-project-2
          jwk-set-uri: ${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs #optional
#          jwk-set-uri: http://localhost:8080/realms/myauth/protocol/openid-connect/certs
# Keycloak configuration
keycloak:
  realm: mini-project-2
  resource: mini-project-2
  auth-server-url: http://localhost:8080/
  credentials:
    secret: DJk2UQEvfm4ZK7bxzxhF0C4zO0RQVAhk
    client-id: mini-project-2
    provider: secret
springdoc:
  swagger-ui:
    oauth:
      client-id: myauth
      client-secret: CjAPGgpaxeIkeU09QORygtzHRa6i4ZTS
      realm: myauth
    tags-sorter: alpha
